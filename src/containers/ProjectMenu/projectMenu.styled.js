import { Section } from '@ynput/ayon-react-components'
import styled from 'styled-components'

export const ProjectMenu = styled(Section)`
  h3 {
    border: none;
  }

  menu {
    padding: 0;
  }

  .menu-item {
    justify-content: space-between;
    gap: 40px;
  }
`

export const All = styled.div`
  overflow: hidden;
`
